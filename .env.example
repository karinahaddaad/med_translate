# Full URL to your deployed translation API (e.g. an NLLB Modal endpoint).
# Must accept POST with JSON {text} for text mode, multipart {audio} for audio mode.
VITE_TRANSLATION_ENDPOINT=https://your-modal-app.modal.run/translate
